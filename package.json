{
  "name": "titogo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm:start --prefix backend\" \"npm:run dev --prefix frontend -- --port 3000\"",
    "start:backend": "npm start --prefix backend",
    "start:frontend": "npm run dev --prefix frontend -- --port 3000",
    "build:frontend": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
