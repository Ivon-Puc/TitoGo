# Porta onde o servidor Node.js vai rodar
PORT=5000

# Endereço da base de dados (preencha com suas credenciais)
# Exemplo: postgresql://user:password@localhost:5433/flow
DATABASE_URL=postgresql://<user>:<password>@localhost:5433/flow

# Chave secreta para os tokens JWT (não comitar valores reais)
JWT_SECRET=your_jwt_secret_here

# Observação: Não coloque segredos neste arquivo em repositórios públicos. Copie
# este arquivo para `backend/.env` e preencha com os valores reais antes de rodar.
